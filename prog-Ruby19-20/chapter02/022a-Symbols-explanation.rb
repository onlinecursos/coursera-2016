# Symbols are simply constant names that you don't have to predeclafre and that
# are guaranteed to be uni	que. A symbol literal starts with a colon an is
# normally followed vy a some kind of name:

# walk(:north)
# look(:east)

def walk(direction)
	if direction == :north
		# ..
	end
end